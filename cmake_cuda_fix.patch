--- CMakeLists.txt.orig
+++ CMakeLists.txt
@@ -69,6 +69,13 @@
 
 # Link libraries for the shared library
 target_link_libraries(range_libc
   gflags
   pthread
 )
+
+# Link CUDA runtime when CUDA is enabled
+if(WITH_CUDA)
+  set_property(TARGET range_libc PROPERTY CUDA_RESOLVE_DEVICE_SYMBOLS ON)
+  target_link_libraries(range_libc CUDA::cudart)
+endif()
